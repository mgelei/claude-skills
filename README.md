# claude-skills
Collection of useful and semi-useful skills for Claude

## Looking Glass

After downloading `looking-glass.zip`, add your `.env` to the package (see `.env.example` inside the zip for the template). You will need a Google Cloud API key and project ID with the Vertex AI API enabled.

```sh
zip looking-glass.zip .env
```
